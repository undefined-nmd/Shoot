// Import services
import { RequestService, SubjectService, CommentService } from '../services'

let sortRequestsByDate = async (requests, sortType = "ASC") => {

    requests.forEach(request => {
        console.log(request)
    });
    return "Bitches be crazy"

}

export default sortRequestsByDate